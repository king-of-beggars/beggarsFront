import React from 'react'
import { ErrorBoundary } from 'react-error-boundary'

import { useGlobalVariables } from 'providers'

function MainErrorRender({}) {
  return (
    <div>MainErrorRender</div>
  )
}

export default MainErrorRender